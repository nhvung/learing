// ── Lesson 04 Exercises ───────────────────────────────────────────────────────

// Exercise 1: Create a class `BankAccount` with:
//   - Properties: Owner (string), Balance (decimal, read-only outside)
//   - Methods: Deposit(decimal), Withdraw(decimal) — throw if overdraft
//   - Override ToString()
// Create two accounts and transfer money between them.
// TODO

// Exercise 2: Create an abstract class `Shape` with abstract method `Area()`.
// Derive `Circle(double radius)` and `Rectangle(double w, double h)`.
// Put shapes in an array and print each area using foreach.
// TODO

// Exercise 3: Create a record `Color(byte R, byte G, byte B)`.
// Show that two Color instances with the same values are equal (==).
// Use `with` to create a darker version (halve each component).
// TODO

// Exercise 4: Create a `Temperature` class where the value is stored in Celsius
// but can be read in Fahrenheit via a read-only property.
// Formula: F = C * 9/5 + 32
// TODO

// Exercise 5: Add a static `Count` property to the `BankAccount` class from
// Exercise 1 that tracks how many accounts have been created.
// TODO
