# Lesson 04 – OOP & Records

## Topics
- Classes with properties (auto-properties, init-only)
- Primary constructors (C# 12)
- Constructors and `this`
- Inheritance and `override`
- `sealed` classes
- `record` types and `with` expressions
- `record struct`
- `static` members and static classes
- `partial` classes (brief mention)
- `object.ToString()` / `Equals` / `GetHashCode` override

## Run

```bash
cd src && dotnet run
cd exercises && dotnet run
```
