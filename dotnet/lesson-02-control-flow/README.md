# Lesson 02 – Control Flow

## Topics
- `if` / `else if` / `else`
- Switch statement vs switch expression
- Pattern matching in switch (`is`, type patterns, `when` guards)
- `for`, `while`, `do-while`, `foreach`
- `break`, `continue`, `return`
- Ranges and indices (`^1`, `1..3`)

## Run

```bash
cd src && dotnet run
cd exercises && dotnet run
```
