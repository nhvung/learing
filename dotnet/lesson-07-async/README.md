# Lesson 07 – Async & Tasks

## Topics
- `Task` and `Task<T>`
- `async` / `await`
- `Task.Run` for CPU-bound work
- `Task.WhenAll`, `Task.WhenAny`
- `CancellationToken`
- `ValueTask<T>` (when to prefer)
- Exception handling in async code
- Simulating I/O with `Task.Delay`
- `HttpClient` basics (real HTTP call)
- `async Main` entry point

## Run

```bash
cd src && dotnet run
cd exercises && dotnet run
```
