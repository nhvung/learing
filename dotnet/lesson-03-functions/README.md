# Lesson 03 – Functions & Methods

## Topics
- Static methods, parameters, return values
- Optional parameters and named arguments
- `params` arrays
- Method overloading
- Local functions
- Expression-bodied members (`=>`)
- Lambda expressions (`Func<>`, `Action<>`, `Predicate<>`)
- Delegates and first-class functions
- `ref`, `out`, `in` parameters
- Tuples and deconstruction

## Run

```bash
cd src && dotnet run
cd exercises && dotnet run
```
