version: "3.9"

services:
  app:
    build: .
    image: java-course-app:latest
    environment:
      - APP_VERSION=1.0
    restart: "no"

  # Example: add a database service alongside the Java app
  # Uncomment when working on Lesson 8 (JDBC)
  # db:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_DB: coursedb
  #     POSTGRES_USER: student
  #     POSTGRES_PASSWORD: secret
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - db-data:/var/lib/postgresql/data

# volumes:
#   db-data:
