# Lesson 01 – Introduction & Types

## Topics
- `print()`, `input()`
- Built-in types: `int`, `float`, `bool`, `str`, `None`
- f-strings and string methods
- `type()`, `isinstance()`
- Variables, multiple assignment, `*rest` unpacking
- `match` / `case` (Python 3.10+)
- Type conversions: `int()`, `str()`, `float()`, `bool()`

## Run

```bash
python main.py
```

## Exercise

```bash
python exercise.py
```
