# Lesson 06 – Iterators, Generators & Comprehensions

## Topics
- Iterator protocol (`__iter__`, `__next__`)
- `yield` and generator functions
- `yield from` — delegating to sub-generators
- Generator expressions vs list comprehensions (memory trade-off)
- `itertools`: `islice`, `chain`, `groupby`, `combinations`, `product`, `cycle`
- `zip`, `zip_longest`, `enumerate`
- `sorted()` with `key=`, `operator.attrgetter`
- Lazy pipelines

## Run

```bash
python main.py
python exercise.py
```
